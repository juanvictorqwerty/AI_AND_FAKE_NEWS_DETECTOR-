# Plan: Fix Binary Score Logic for Human-Generated Images

## Goal
Modify the database service and related code to correctly store 1 when the analysis marks an image as human-generated (real) and 0 otherwise (AI-generated/fake).

## Current State
- The code in `analysis/main.py` is already attempting to store a binary score (1 for human, 0 for AI).
- However, the condition checks for `prediction_str == 'human'`, but the AI model (`Organika/sdxl-detector`) labels human-generated images as `'real'` and AI-generated as `'sdxl'`.
- This means human images are incorrectly getting score=0 instead of 1.

## Steps
1. Update the score calculation in `analyze_photo` function (around line 148-149) to check `prediction_str == 'real'` instead of `'human'`.
2. Update the score calculation in `analyze_video` function (around line 217-218) to check `prediction_str == 'real'` instead of `'human'`.
3. Verify that the database schema supports storing integer scores (it already does via the `score` column).
4. Test the changes to ensure human-generated images get score=1 and AI-generated get score=0.

## Questions for User
- Confirm that `'real'` is the correct label for human-generated images from the model. Is there any chance it could be different?
- Should we handle any other prediction values besides `'real'` and `'sdxl'`? Currently, anything not `'real'` gets 0.

## Risk Assessment
- Low risk: Only changing the string comparison for determining the binary score.
- The database column `score` is already integer and can store 1/0.
- No schema changes needed since it's managed by Drizzle.</content>
<parameter name="filePath">/media/linux/fba0f54e-b8a2-fb4f-802f-cd9ac70332131/AI_AND_FAKE_NEWS_DETECTOR/.kilo/plans/1775212042279-mighty-engine.md